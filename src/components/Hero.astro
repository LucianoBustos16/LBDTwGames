---

interface CallToAction {
  text: string;
  href: string;
}

export interface Props {
  title?: string;
  subtitle?: string;
  callToAction?: string | CallToAction;
  callToAction2?: string | CallToAction;
  image?: string | any; // TODO: find HTMLElementProps
}

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  callToAction = await Astro.slots.render('callToAction'),
  callToAction2 = await Astro.slots.render('callToAction2'),
  image = await Astro.slots.render('image'),
} = Astro.props;
---

<!-- <section>
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <div class="py-12 md:py-20">
      <div class="text-center pb-10 md:pb-16 max-w-5xl mx-auto">
            <h1
              class="text-5xl md:text-[3.50rem] font-bold leading-tighter tracking-tighter mb-4 font-heading"              
            >
            LBDTw<span class="text-cyan-600">+</span>
            </h1>

        <div class="max-w-3xl mx-auto">
          <p class="text-xl font-medium text-gray-500 mb-8 dark:text-slate-400">Un espacio hecho con <span class=" line-through">cero</span> ❤️ y esperamos que lo disfrutes😉.
            Te traemos los juegos gratis que están por todo internet para que puedas disfrutarlos.</p>
        </div>
      </div>
      <div>
        
            <div class="relative m-auto max-w-4xl">
              <img src="src/assets/images/hero.png" alt="">
            </div>
      </div>
    </div>
  </div>
</section> -->

<div class="relative grid h-full place-items-center sm:grid-cols-2 p-8 my-6">
    <h1 class=" text-center font-bold text-8xl backdrop-blur-md bg-white/10 rounded-2xl p-5 ">
        LBDTw<span class="text-cyan-600 font-extrabold">+</span>
      <div class="gradient-text text-center font-extrabold tracking-tighter text-4xl ">
        <p class="text-xl font-medium text-gray-500 mb-8 dark:text-slate-400">Un espacio hecho con <span class=" line-through">cero</span> ❤️ y esperamos que lo disfrutes😉.
            Te traemos los juegos gratis que están por todo internet para que puedas disfrutarlos.</p>
      </div>
    </h1>
    <div id="astronaut" class="w-2/3 max-w-3xl self-start sm:w-10/12 sm:self-auto sm:justify-self-start ">
      <picture>
	<img src="src/assets/images/hero2.avif" loading="eager" decoding="async" alt="A floating astronaut in a space suit" class="object-cover w-full h-full ">
</picture>
    </div>
  </div>


  <style>
  @keyframes float {
    0% {
      transform: translate3d(0, 30px, 0);
    }
  }

  #astronaut {
    animation: float linear 2.5s infinite alternate;
  }
  </style>